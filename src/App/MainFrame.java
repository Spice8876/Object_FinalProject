package App;

import InGame.BackGroundPanel;
import InGame.InGamePanel;
import MusicSelect.MusicSelect;
import javax.swing.*;

public class MainFrame extends JFrame {
/*
    InGamePanel inGamePanel;
    MusicSelect musicSelect;

    public MainFrame(){
        super("리듬 게임");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        inGamePanel = new InGamePanel();
        musicSelect = new MusicSelect();
        setContentPane(musicSelect); // GamePanel을 컨텐트팬으로 등록한다.
        pack(); //하위 컨탠트에 맞추는 설정
        setVisible(true);
    }

    public void MoveToInGame(){
        getContentPane().removeAll();
        setContentPane(inGamePanel);
        revalidate();
        repaint();
        inGamePanel.start();
    }

    public void onGameOver(){
        getContentPane().removeAll();
        setContentPane(musicSelect);
        revalidate();
        repaint();
    }*/
}
